import React, { FC } from 'react';

type GalaxisFooterProps = {
  url: string;
};

const GalaxisFooter: FC<GalaxisFooterProps> = () => {
  return <div>This is the footer component.</div>;
};

export default GalaxisFooter;
